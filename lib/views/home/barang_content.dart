import 'package:flutter/material.dart';

class BarangContent extends StatelessWidget {
  const BarangContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Belum ada barang'),
    );
  }
}